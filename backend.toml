[server]
bind = "127.0.0.1:3000"

[storage]
artifacts_root = "artifacts"

[storage.mysql]
host = "10.3.0.2"
port = 3306
database = "dynamic_audit_platform"
username = "artisan_dap"
# Ahh everyones leaked a password once right ?
password = "E*j$A^52734svN^$83N9"
max_connections = 10

[cache]
freshness_window_seconds = 30
dedupe_inflight = true
ct_subdomain_cache_ttl_seconds = 86400
# 86400

[engine]
rules_path = "rules.yaml"
plugins_path = "plugins"
default_scope_mode = "domain_sweep"
force_single_site_for_hostnames = false
enabled_tests = []
disabled_tests = []

[engine.discovery_probes]
api_endpoints = true
dav_endpoints = true

worker_poll_interval_ms = 1000

[engine.execution]
max_concurrent_tests = 10
max_workers = 48
per_host_concurrency = 24

[engine.report]
formats = ["json"]

[engine.psi]
enabled = true
strategies = ["mobile", "desktop"]
categories = ["performance", "accessibility", "best-practices", "seo"]
timeout_seconds = 120
#credentials_file = "/home/dwhitfield/Developer/Artisan_Hosting/Software/dap-backend/.gsa"
